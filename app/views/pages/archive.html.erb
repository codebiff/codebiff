<h1>Archive</h1>

<% @posts.sort.reverse.each do |month, posts| %>
  
  <h4><%= month.strftime("%B %Y") %></h4>
  <ul>
    <% for post in posts %>
      <li><%= link_to post.title, post.slug %></li>
    <% end %>
  </ul>
  
<% end %>